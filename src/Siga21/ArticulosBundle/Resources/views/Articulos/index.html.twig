<h1>Articulos list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Referencia</th>
            <th>Nombre</th>
            <th>Descripcion</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('articulos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.referencia }}</td>
            <td>{{ entity.nombre }}</td>
            <td>{{ entity.descripcion }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('articulos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('articulos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('articulos_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
