<h1>Tareas list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Fecha</th>
            <th>Socio</th>
            <th>Notas</th>
            <th>Terminada</th>
            <th>Actions</th>
            <th>Eso</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('tareas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.Nombre }}</td>
            <td>{% if entity.Fecha %}{{ entity.Fecha|date('Y-m-d H:i:s') }}{% endif%}</td>
            <td>{{ entity.socios }}</td>
            <td>{{ entity.Notas }}</td>
            <td>{{ entity.Terminada }}</td>
            <td>{{ entity.Asociados.nome }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('tareas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tareas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('tareas_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
